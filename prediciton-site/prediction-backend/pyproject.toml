[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "prediction-backend"
version = "0.1.0"
description = "FastAPI AMM backend"
requires-python = ">=3.10"
dependencies = []


